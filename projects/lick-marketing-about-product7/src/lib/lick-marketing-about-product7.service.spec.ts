import { TestBed } from '@angular/core/testing';

import { LickMarketingAboutProduct7Service } from './lick-marketing-about-product7.service';

describe('LickMarketingAboutProduct7Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingAboutProduct7Service = TestBed.get(LickMarketingAboutProduct7Service);
    expect(service).toBeTruthy();
  });
});
