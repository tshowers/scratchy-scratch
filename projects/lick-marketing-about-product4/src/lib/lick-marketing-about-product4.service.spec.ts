import { TestBed } from '@angular/core/testing';

import { LickMarketingAboutProduct4Service } from './lick-marketing-about-product4.service';

describe('LickMarketingAboutProduct4Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingAboutProduct4Service = TestBed.get(LickMarketingAboutProduct4Service);
    expect(service).toBeTruthy();
  });
});
