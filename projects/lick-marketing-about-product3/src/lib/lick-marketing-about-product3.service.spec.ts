import { TestBed } from '@angular/core/testing';

import { LickMarketingAboutProduct3Service } from './lick-marketing-about-product3.service';

describe('LickMarketingAboutProduct3Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingAboutProduct3Service = TestBed.get(LickMarketingAboutProduct3Service);
    expect(service).toBeTruthy();
  });
});
