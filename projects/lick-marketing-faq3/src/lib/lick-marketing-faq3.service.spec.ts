import { TestBed } from '@angular/core/testing';

import { LickMarketingFaq3Service } from './lick-marketing-faq3.service';

describe('LickMarketingFaq3Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingFaq3Service = TestBed.get(LickMarketingFaq3Service);
    expect(service).toBeTruthy();
  });
});
