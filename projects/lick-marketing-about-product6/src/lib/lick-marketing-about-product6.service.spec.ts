import { TestBed } from '@angular/core/testing';

import { LickMarketingAboutProduct6Service } from './lick-marketing-about-product6.service';

describe('LickMarketingAboutProduct6Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingAboutProduct6Service = TestBed.get(LickMarketingAboutProduct6Service);
    expect(service).toBeTruthy();
  });
});
