import { TestBed } from '@angular/core/testing';

import { LickMarketingReviews3Service } from './lick-marketing-reviews3.service';

describe('LickMarketingReviews3Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingReviews3Service = TestBed.get(LickMarketingReviews3Service);
    expect(service).toBeTruthy();
  });
});
