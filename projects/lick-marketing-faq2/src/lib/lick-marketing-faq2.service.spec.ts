import { TestBed } from '@angular/core/testing';

import { LickMarketingFaq2Service } from './lick-marketing-faq2.service';

describe('LickMarketingFaq2Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingFaq2Service = TestBed.get(LickMarketingFaq2Service);
    expect(service).toBeTruthy();
  });
});
