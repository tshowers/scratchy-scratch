import { TestBed } from '@angular/core/testing';

import { LickMarketingContactUs1Service } from './lick-marketing-contact-us1.service';

describe('LickMarketingContactUs1Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingContactUs1Service = TestBed.get(LickMarketingContactUs1Service);
    expect(service).toBeTruthy();
  });
});
