import { TestBed } from '@angular/core/testing';

import { LickMarketingHeader4Service } from './lick-marketing-header4.service';

describe('LickMarketingHeader4Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingHeader4Service = TestBed.get(LickMarketingHeader4Service);
    expect(service).toBeTruthy();
  });
});
