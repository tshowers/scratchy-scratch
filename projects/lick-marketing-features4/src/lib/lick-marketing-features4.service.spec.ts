import { TestBed } from '@angular/core/testing';

import { LickMarketingFeatures4Service } from './lick-marketing-features4.service';

describe('LickMarketingFeatures4Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingFeatures4Service = TestBed.get(LickMarketingFeatures4Service);
    expect(service).toBeTruthy();
  });
});
