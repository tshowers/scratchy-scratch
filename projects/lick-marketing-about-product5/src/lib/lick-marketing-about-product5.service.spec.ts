import { TestBed } from '@angular/core/testing';

import { LickMarketingAboutProduct5Service } from './lick-marketing-about-product5.service';

describe('LickMarketingAboutProduct5Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingAboutProduct5Service = TestBed.get(LickMarketingAboutProduct5Service);
    expect(service).toBeTruthy();
  });
});
