import { ImageCheckPipe } from './image-check.pipe';

describe('ImageCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
