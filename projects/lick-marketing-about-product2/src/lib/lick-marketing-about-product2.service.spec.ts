import { TestBed } from '@angular/core/testing';

import { LickMarketingAboutProduct2Service } from './lick-marketing-about-product2.service';

describe('LickMarketingAboutProduct2Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingAboutProduct2Service = TestBed.get(LickMarketingAboutProduct2Service);
    expect(service).toBeTruthy();
  });
});
