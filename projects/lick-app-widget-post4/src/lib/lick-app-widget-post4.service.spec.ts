import { TestBed } from '@angular/core/testing';

import { LickAppWidgetPost4Service } from './lick-app-widget-post4.service';

describe('LickAppWidgetPost4Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickAppWidgetPost4Service = TestBed.get(LickAppWidgetPost4Service);
    expect(service).toBeTruthy();
  });
});
