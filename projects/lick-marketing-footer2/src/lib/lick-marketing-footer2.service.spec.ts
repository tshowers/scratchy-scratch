import { TestBed } from '@angular/core/testing';

import { LickMarketingFooter2Service } from './lick-marketing-footer2.service';

describe('LickMarketingFooter2Service', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const service: LickMarketingFooter2Service = TestBed.get(LickMarketingFooter2Service);
    expect(service).toBeTruthy();
  });
});
